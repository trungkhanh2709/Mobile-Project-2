<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- Điều chỉnh radius để làm tròn góc -->
    <solid android:color="@android:color/transparent" /> <!-- Background trong suốt -->
    <stroke
        android:width="2dp"
    android:color="#252424" />
</shape>
